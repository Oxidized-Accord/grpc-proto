syntax = "proto3";
package AuthService;

service AuthService {
  rpc Login(LoginReq) returns (LoginRes);
}

message LoginReq {
  string username = 1;
  string password = 2;
}
message LoginRes {
  string refresh_token = 1;
  string jwt = 2;
}
